# API Contracts

## Escopo consolidado
- integrações do módulo de relatórios

## Evidências consolidadas
- request:POST /api/reports/export
- request:GET /api/reports/export/:id

## Conteúdo reconciliado
- `POST /api/reports/export`: cria job assíncrono de exportação
- `GET /api/reports/export/:id`: consulta status e link do arquivo

## Itens pendentes de validação
- payload completo do filtro não confirmado

## Conflitos e gaps relacionados
- Nenhum
